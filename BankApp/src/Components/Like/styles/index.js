import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  absoluteStore: {
    width: 45,
    height: 45,
    borderRadius: 290,
    marginLeft: 25,
    marginTop: 20,
    position: 'absolute',
    overflow: 'hidden',
  },
  Image: {
    width: 800,
    height: 800,
  },
});

export default styles;
