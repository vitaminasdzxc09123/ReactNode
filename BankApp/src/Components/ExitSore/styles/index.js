import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  absoluteStore: {
    width: 45,
    height: 45,
    borderRadius: 290,
    marginLeft: 210,
    marginTop: 20,
    position: 'absolute',
    overflow: 'hidden',
  },
  Image: {
    width: 600,
    height: 600,
    position: 'absolute',
  },
});

export default styles;
