import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  TextAdd: {
    position: 'absolute',
    marginTop: 35,
    marginLeft: 210,
    fontSize: 17,
  },
});

export default styles;
