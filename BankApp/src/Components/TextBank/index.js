import React from 'react';
import {Text} from 'react-native';
import styles from '../TextBank/styles';

const TextBank = () => {
  return <Text style={styles.TextAdd}>Add bank</Text>;
};

export default TextBank;
